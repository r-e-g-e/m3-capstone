import { CirculosSistemasDeCores } from "./styles";

// Circulos da pagina sobre, são das cores que indicam qual a importancia do card
function CirculoSistemaCores({ color }) {
  return <CirculosSistemasDeCores color={color}></CirculosSistemasDeCores>;
}

export default CirculoSistemaCores;
